%plotting particles in
clear alll; close all; clc;
load pos.out
part = zeros(1000,3);
for jj = 1:4
    for ii = 1:1000
        part(jj,ii,1) = pos(jj,ii,1);
        part(jj,ii,2) = pos(jj,ii,2);
        part(jj,ii,3) = pos(jj,ii,3);
    end 
end
figure(1)
scatter3(part(:,1),part(:,2),part(:,3),)